import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  title: {
    fontSize: 20,
    letterSpacing: 6,
    paddingVertical: 20,
    textAlign: "center",
  },
});

export default styles;
